// threshold-app — interactive Mac dev shell (phase 4).
import SwiftUI

@main
struct ThresholdApp: App {
    var body: some Scene {
        WindowGroup("Threshold") {
            Text("threshold-app scaffold")
        }
    }
}
